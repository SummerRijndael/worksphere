--- app/Services/AvatarService.php
+++ app/Services/AvatarService.php
@@ -73,6 +73,12 @@
         // Priority 1: Media Library
         if ($user->hasMedia('avatars')) {
             $media = $user->getFirstMedia('avatars');
+            
+            // If disk is not local/public, we assume the file exists
+            // (since file_exists don't work on remote flysystem disks like s3/r2)
+            if ($media->disk !== 'local' && $media->disk !== 'public') {
+                $url = $media->getUrl($variant);
+            } else {
                 $path = $media->getPath($variant);
 
                 // Check if file exists on disk
@@ -95,6 +101,7 @@
 
                     file_put_contents($logPath, $logMessage, FILE_APPEND);
                 }
+            }
         }
 
         return new AvatarData(
